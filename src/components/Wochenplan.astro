---
import { Icon } from 'astro-icon/components';
import { wochenplan, standDerDaten } from '../data/verein';

interface Props {
  /** Auf der eigenen Unterseite uebernimmt der Seitenkopf die Ueberschrift. */
  ohneKopf?: boolean;
}
const { ohneKopf = false } = Astro.props;

---

<section class="section section--sand" id="trainingszeiten">
  <div class="container">
    {!ohneKopf && (
      <div class="plan__kopf reveal">
        <div class="section-head">
          <p class="eyebrow">Trainingszeiten</p>
          <h2>Die Woche<br />beim SCN</h2>
          <p class="lead">
            Montag bis Freitag ist bei uns etwas los. Such dir raus, was passt, und komm vorbei.
          </p>
        </div>
      </div>
    )}


    <div class="plan reveal">
      {wochenplan.map((tag) => (
        <div class="tag">
          <h3 class="tag__name">{tag.tag}</h3>
          <ul class="tag__liste">
            {tag.eintraege.map((e) => (
              <li>
                <a href={`/sportarten/${e.slug}/`}>
                  <span class="zeit">{e.zeit}</span>
                  <span class="was">{e.was}</span>
                  <span class:list={['ort', `ort--${e.ort.toLowerCase()}`]}>
                    <span class="punkt" aria-hidden="true"></span>{e.ort}
                  </span>
                  <Icon class="pfeil" name="tabler:arrow-right" aria-hidden="true" />
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <p class="plan__stand">Stand: {standDerDaten}</p>
  </div>
</section>

<style>
  .plan__kopf {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    justify-content: space-between;
    gap: 1.8rem;
    margin-bottom: clamp(2.2rem, 4vw, 3rem);
  }
  .plan__kopf h2 { font-size: var(--step-4); }


  /* Farbpunkt zeigt den Ort an */
  .punkt {
    width: 9px;
    height: 9px;
    flex: none;
    border-radius: 50%;
    background: var(--scn-blue);
  }
  .ort--platz .punkt { background: var(--scn-red); }
  .ort--vereinsheim .punkt { background: var(--scn-gold-dark); }

  /* ---------- Wochenraster ---------- */
  .plan { display: grid; gap: 1rem; align-items: start; }
  @media (min-width: 52rem) { .plan { grid-template-columns: repeat(5, 1fr); gap: 0.9rem; } }

  .tag {
    background: var(--paper);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 1px 2px rgba(11, 23, 32, 0.06);
  }

  .tag__name {
    background: var(--scn-gold);
    color: var(--ink);
    font-size: 1.05rem;
    letter-spacing: 0.04em;
    padding: 0.6rem 1rem;
    margin: 0;
  }

  .tag__liste { list-style: none; margin: 0; padding: 0.3rem 0 0.4rem; }
  .tag__liste li + li { border-top: 1px solid var(--line); }

  .tag a {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 0.1rem 0.5rem;
    padding: 0.7rem 1rem;
    text-decoration: none;
    color: inherit;
    transition: background-color 0.16s ease;
  }
  /* Kontrastreicher Wechsel statt zarter Tönung – die Zeile setzt sich
     beim Überfahren deutlich ab. */
  .tag a:hover { background: var(--ink); }
  .tag a:hover .was { color: #fff; }
  .tag a:hover .zeit,
  .tag a:hover .ort { color: rgba(255, 255, 255, 0.7); }
  .tag a:hover .pfeil { color: var(--scn-gold); }
  /* Punkte auf dunklem Grund etwas aufhellen, damit sie sichtbar bleiben */
  .tag a:hover .punkt { background: color-mix(in srgb, var(--scn-blue) 65%, white); }
  .tag a:hover .ort--platz .punkt { background: color-mix(in srgb, var(--scn-red) 75%, white); }
  .tag a:hover .ort--vereinsheim .punkt { background: var(--scn-gold); }

  .zeit {
    grid-column: 1 / -1;
    font-size: 0.76rem;
    font-weight: 700;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.04em;
    color: var(--text-muted);
  }
  .was {
    grid-column: 1 / -1;
    font-weight: 600;
    font-size: 0.93rem;
    line-height: 1.3;
    text-wrap: balance;
  }

  .ort {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-top: 0.25rem;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .pfeil {
    align-self: end;
    justify-self: end;
    font-size: 0.95rem;
    color: var(--scn-red);
    opacity: 0;
    transform: translateX(-3px);
    transition: opacity 0.16s ease, transform 0.16s ease;
  }
  .tag a:hover .pfeil { opacity: 1; transform: none; }

  .plan__stand {
    margin-top: 1.8rem;
    text-align: right;
    font-size: 0.72rem;
    letter-spacing: 0.03em;
    color: var(--text-muted);
  }
</style>
